<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:name="com.rachmad.app.league.ui.classement.ClassementItemFragment"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <HorizontalScrollView
        android:id="@+id/classement_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <de.codecrafters.tableview.TableView
            android:id="@+id/classement_list"
            android:layout_width="600dp"
            android:layout_height="match_parent"
            app:tableView_columnCount="7"
            app:tableView_headerColor="@color/colorPrimary"
            app:tableView_headerElevation="0"
            tools:context=".ui.classement.ClassementItemFragment"
            tools:listitem="@layout/fragment_classement_item" />
    </HorizontalScrollView>

    <LinearLayout
        android:id="@+id/loadingLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:background="@color/white"
        android:visibility="gone"
        android:orientation="vertical">

        <ProgressBar
            android:id="@+id/loading"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

        <TextView
            android:id="@+id/error"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="8dp" />
    </LinearLayout>
</FrameLayout>